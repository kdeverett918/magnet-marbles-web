# Magnet Marbles Web Launch Readiness

Last updated: 2026-06-22

## Target

Phone-first browser 3D arcade arena. Comparable quality bar:

- Marble It Up / toy-marble clarity: glossy readable marbles, clean table silhouette, instant motion readability.
- My Street marble minigame: collect, steal, bank, and goal ownership must be clear in the first round.
- Mobile arcade web games: first playable screen in a few seconds, low-chrome HUD, stable portrait controls, no confusing dead-end online flows.

AA web pass condition: a new mobile player can start from the menu, understand collect-to-bank within 30 seconds, finish a round without UI overlap, and return/rematch cleanly; desktop and mobile builds pass lint/build and screenshot smoke checks.

## Current Evidence

- `npm run launch:check` passes locally and writes `outputs/launch-check.json`; the latest shared-browser local gate was `LAUNCH_SKIP_LIVE_ONLINE=1 LAUNCH_CHECK_LIVE_WEB=0 LAUNCH_CHECK_LIVE_ONLINE_MODES=0 LAUNCH_CHECK_MOBILE_PERF=1 LAUNCH_OUTPUT=outputs/launch-check-progression-mobile-perf.json npm run launch:check`, captured at `2026-06-21T23:57:03.697Z`. It ran 22 steps: web tests/typecheck/lint/audit/build, production `dist/` metadata/installability smoke, generated-asset smoke, server build plus full server audit, local service readiness checks, one shared headless Chrome/CDP browser, dev-server accessibility smoke, production-preview smoke with progression UI assertions, all-modes production smoke, production-preview performance smoke, throttled mobile performance smoke, production-preview browser soak, local online smoke, local online all-modes smoke, and local online disconnect takeover smoke. Public-web smoke is available with `LAUNCH_CHECK_LIVE_WEB=1`; live all-modes backend smoke is available with `LAUNCH_CHECK_LIVE_ONLINE_MODES=1`.
- Web `npm run build`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm audit` pass locally. Current suite: 140 tests.
- After the explicit magnet-mastery, neutral touch-dash memory, haul-weight speed-risk, pre-contact affordance badge, touch-control, background-music removal, runtime removed-music cache/DOM guard, quieter SFX output-trim pass, legacy saved-volume migration pass, later-round advanced powerup ramp, menu powerup-ramp communication pass, root crash-recovery and support-code intake pass, SFX volume-control, SFX preview-control, haptics preview-control, Color Assist readability pass, player Motion control pass, HUD objective/meter accessibility pass, no-browser accessibility static smoke, no-browser mobile layout/touch static smoke, first-round Pull / Carry / Bank coach strip, pre-round mode briefing pass, competitive race-status pass, carry-risk/steal-target advice pass, rim-danger readability pass, right-thumb action-state readability pass, daily-streak retention pass, next-unlock progression target pass, local mode records pass, no-browser sim-performance budget pass, safe-gate sim-soak evidence pass, deployed build/menu Candidate stamp evidence hardening, executable negative evidence-template guard, mode-aware results recap, distinct Collector / Bruiser / Banker bot-personality pass, quick-bank streak mastery pass, fair privacy/support launch-copy pass, local data reset control pass, public IP/reference safety pass, mode-aware results, online room-safety changes, HUD human-seat fix, movement-zone dash cleanup, static frontend build metadata emission, source-fingerprint provenance including validation scripts, long no-browser simulation soak, CI no-browser gate upgrade, Render always-on backend blueprint change, phone haptics pass, reduced-motion-safe camera-impact pass, transient feedback-toast pass, cluster-milestone reward pass, solo pause/options lifecycle plus focus-management pass, no-browser live-version preflight pass, three-level bot difficulty pass, skin/trail shader pass, explicit browser-automation opt-in guard, no-browser live deploy monitor, regression-checked clean failure exits for live-stale deploy smokes, live removed-music verification, app-shell service-worker resilience with production-bundle registration coverage, service-worker behavior smoke coverage, Render backend health-check repair, physical-device evidence gate, Render hosting-config gate, human AA review evidence gate, release evidence template generation, Candidate stamp release-proof pass, stricter old-music tombstone handling, broader live removed-music variant preflight, conditional no-browser release-status dashboard, stale-live review-start gating, result mastery badge pass, and post-match Unlock & Equip reward CTA pass, browser automation was intentionally paused to avoid opening more Chrome tabs. The latest no-browser validation passed at `2026-06-22T13:27:50.994Z`: `npm run validate:no-browser`, covering `32` steps: `npm run test` (`21` files, `140` tests), `npm run vertical:slice` (`9` tests), `npm run sim:soak` (`15` mode/seed runs and `2,700` simulated seconds), `npm run sim:perf` (`10` all-mode budget runs), `npm run lint`, web/server `npm audit` (`0` vulnerabilities), `npm run build`/typecheck, dist build-info, source-fingerprint, metadata smoke, service-worker behavior smoke, public IP-safety smoke, payload-budget smoke, SFX-only asset smoke, no-browser accessibility static smoke, no-browser mobile layout/touch static smoke, AA-readiness static smoke, browser-launch/tab guard smoke, clean-exit smoke, evidence template generation, executable negative evidence-template guard, device QA checklist smoke, hosting config smoke, human AA review checklist smoke, `npm --prefix server run build`, local online Classic/all-modes/disconnect smokes against a temporary localhost server, and `git diff --check`. It writes `outputs/no-browser-check.json` with `browserAutomation: false` and starts no Chrome/CDP browser or tabs. The online subchecks verified Classic join/snapshot, all five requested mode snapshots, wrong-mode private-room rejection, abandoned-seat bot takeover/replacement-seat reuse, and matching local server source fingerprint `f7e0a662220fc3ae`. The full browser launch gate must be rerun deliberately with `MM_ALLOW_BROWSER=1` before claiming this candidate as release-verified.
- `npm run preview:smoke` passes locally against the built `dist/` served through Vite preview and writes `outputs/preview-smoke.json` plus menu and gameplay screenshots. Latest shared-CDP launch-check run at `2026-06-21T23:56:04.067Z` loaded production `/assets/*` only, found no Vite dev client/source modules, verified the stars/daily/skin-trail progression UI (`12★`, Daily Challenge, Gold Rush equipped), showed all five requested modes (`Classic`, `Battle`, `King Magnet`, `Team Bank`, `Survival`), kept Single Player / Play Online in the first mobile viewport, verified visible Privacy / Support launch links in the mobile viewport, verified every mode card is fully visible in the 390x844 first viewport, waited out the menu fade before screenshot capture, entered gameplay, and confirmed the canvas/HUD/control view.
- `npm run modes:smoke` passes locally against the built `dist/` served through Vite preview and writes `outputs/modes-smoke.json` plus one 390x844 screenshot per mode in `outputs/modes-smoke/`. Latest run booted `Classic`, `Battle`, `King Magnet`, `Team Bank`, and `Survival`, verified each mode-specific objective, checked Team Bank team labels, checked Survival lives, and confirmed accessible gameplay controls in each mode.
- `npm run perf:smoke` passes locally against a 390x844 headless Chrome gameplay sample and writes `outputs/perf-smoke.json`. Latest shared-CDP launch-check run at `2026-06-21T23:56:23.476Z` used the production preview URL: p95 frame time 4.3 ms, 1 frame over 33 ms, and 18.2 MB used JS heap.
- `npm run perf:mobile:smoke` passes locally against production preview and writes `outputs/mobile-perf-smoke.json`. Latest opt-in shared-CDP launch-check run at `2026-06-21T23:56:40.833Z` used a 390x844 mobile viewport, device scale factor 2, 4x CPU throttling, and the mobile-safe Lite profile: p95 frame time 33.4 ms, p99 frame time 47.9 ms, 3 frames over 50 ms, no console errors or warnings, and 17.5 MB used JS heap. A focused high-quality stress rerun with `MOBILE_PERF_QUALITY=high MOBILE_PERF_OUTPUT=outputs/mobile-perf-smoke-high.json npm run perf:mobile:smoke` also passed after capping phone DPR and disabling ambient motes on phone-sized High mode: p95 frame time 45.7 ms, p99 frame time 60.3 ms, and 7 frames over 50 ms.
- `npm run soak:smoke` passes locally against the production preview and writes `outputs/browser-soak-smoke.json`. Latest shared-CDP launch-check run at `2026-06-21T23:56:57.869Z` drove keyboard movement, magnet, dash, and powerup inputs for 15 seconds in a 390x844 viewport, kept the HUD/canvas/controls alive, recorded no console errors or warnings, and saw p95 frame time 4.3 ms, p99 frame time 6.3 ms, 2 frames over 33 ms, and 23.8 MB used JS heap.
- `npm run a11y:smoke` passes locally and writes `outputs/a11y-smoke.json`. Latest run verifies keyboard focus through menu controls, screen-reader-only input guidance, Space activation into gameplay, keyboard movement/magnet input, accessible gameplay controls, results-menu navigation, and online retry focus/state.
- `npm run a11y:static` passes without browser automation and writes `outputs/a11y-static-smoke.json`; it is now part of `validate:no-browser`. Latest safe-gate run at `2026-06-22T13:07:56.750Z` passed `10/10` checks covering screen-reader menu guidance, focus-visible CSS, HUD status/meter semantics, accessible touch controls plus compact action-state chips, pause/results labels and focus restore, reduced-motion CSS, comfort options, pure screen-reader model tests, and required manual screen-reader/focus evidence.
- `npm run mobile:layout` is a no-browser static phone-layout/touch contract check and writes `outputs/mobile-layout-static-smoke.json`. Latest no-browser validation run at `2026-06-22T13:07:57.206Z` passed `8/8` checks verifying viewport-fit/safe-area CSS, first-screen menu play actions, candidate stamp visibility, HUD non-overlap with lower-right controls, direct-drag movement plus right-thumb magnet/dash controls with visible action-state chips, thumb-sized primary actions, overlay flow, and the explicit requirement that Android/iOS physical-device evidence still gates launch.
- `npm run metadata:smoke` passes locally and writes `outputs/metadata-smoke.json`; `launch:check` now runs it against `dist/index.html` / `dist/` and writes `outputs/metadata-smoke-dist.json`. Latest no-browser dist run at `2026-06-22T12:27:51.163Z` verifies title, viewport, theme color, description, mobile web-app tags, Open Graph/Twitter share metadata, manifest link, favicon/apple icon links, manifest display/orientation/theme consistency, a real 180x180 Apple touch PNG, 192x192 and 512x512 install PNGs, a 512x512 maskable PNG, a 1200x630 social preview PNG, shipped `privacy.html` / `support.html` launch pages with required no-ads/no-payments/no-loot-boxes, safe-reporting, SFX volume, Test SFX copy, haptics, Test Haptics copy, exact touch-control copy, Reset data copy, Candidate ID guidance, crash support-code guidance for the Table reset needed screen, and `service-worker.js` app-shell coverage.
- `npm run sw:smoke` executes the actual `service-worker.js` in a no-browser fake worker environment. It verifies install precache, stale cache deletion, removed-music `410 no-store` handling for old `music.*` paths, `build.json` network-first cache fallback, offline `index.html` app-shell fallback, SFX/static cache-first behavior, and no interception of cross-origin backend requests.
- `npm run build-info:smoke` statically verifies the production `dist/` bundle and `dist/build.json` include matching `magnet-marbles-web` build metadata with the expected local git commit, branch, dirty flag, ISO build time, and source fingerprint. It is wired into both `validate:no-browser` and `launch:check` so a stale or unversioned frontend bundle is caught before deployment. Latest no-browser run verified the current local commit and source fingerprint.
- `npm run source:fingerprint` verifies that the source fingerprint is deterministic and covers release-critical code/config, validation scripts, generated launch art, the app-shell service worker, no-browser accessibility static smoke, no-browser mobile layout static smoke, physical-device QA gate, hosting-config gate, human AA review gate, release evidence template generator, executable evidence-template negative guard, no-browser release-status dashboard, and shipped binary assets. Latest no-browser run at `2026-06-22T13:27:40.365Z` passed with fingerprint `f7e0a662220fc3ae`, `146` fingerprinted files, PNG launch art coverage, MP3 SFX coverage, `src/vite-env.d.ts`, `src/game/serviceWorker.ts`, `public/service-worker.js`, `scripts/a11y-static-smoke.mjs`, `scripts/mobile-layout-static-smoke.mjs`, `scripts/service-worker-smoke.mjs`, `scripts/evidence-template.mjs`, `scripts/evidence-negative-smoke.mjs`, `scripts/device-qa-smoke.mjs`, `scripts/hosting-config-smoke.mjs`, `scripts/human-review-smoke.mjs`, `scripts/release-status.mjs`, `docs/DEVICE_QA_CHECKLIST.md`, `docs/HUMAN_AA_REVIEW_CHECKLIST.md`, and `scripts/ip-safety-smoke.mjs` plus `scripts/clean-exit-smoke.mjs` included as required files.
- `npm run ip:safety` verifies shipped public/dist launch surfaces do not publish protected reference-game names while allowing internal docs/tests to keep design-reference notes. Latest no-browser validation run at `2026-06-22T05:31:31.614Z` scanned `19` shipped files and passed with `0` shipped violations.
- Generated SFX samples are tracked in `docs/ASSET_BUDGET.md` and ship under `public/audio/sfx/` with WebAudio synth fallback, so loading failures do not block play. Background music playback is intentionally disabled for this candidate; `npm run assets:smoke` verifies the six shipped SFX files and fails if any `audio/music.*` file exists in either `public/` or `dist/`. App boot now also removes stale `audio/music.*` media elements and purges old cached music variants, while the WebAudio master gain defaults to a quieter `28%`, applies an added output trim to all samples/synth fallback, migrates old saved loud SFX volumes down once, and exposes persisted, clamped SFX volume plus Test SFX preview controls in both the main menu and pause options; phone haptics also expose Test Haptics preview controls.
- `npm run dist:budget` verifies production payload budgets, rejects source maps/dev-client leakage, and fails if any `audio/music.*` file ships in `dist/`. Latest no-browser run at `2026-06-22T13:27:41.684Z` passed with `2,866,514` total dist bytes, `1,686,336` raw JS bytes, `468,318` gzipped JS bytes, `54,670` CSS bytes, `138,183` audio bytes, and `972,808` image bytes, including the `3,468` byte service worker. A first Color Assist implementation using Drei `Html` failed this gate by pushing the R3F chunk over the per-file JS budget; the shipped version uses lightweight mesh pip badges instead.
- `npm run vertical:slice` verifies the explicit first-build contract without a browser: the five requested modes, 90-second rounds, 4-player/3-bot starts, distinct Collector / Bruiser / Banker bot roles, Team Bank teams, Survival lives, Easy / Normal / Hard bot difficulty, Magnet Burst / Shock Pulse / Heavy Core first-round launch powerup pool, later-round advanced powerups, six marble skin/trail cosmetics, daily challenge, core magnet/carry tuning including haul-weight speed risk and quick-bank streaks, and a simulated move -> magnetize -> carry -> bank -> bot participation -> match-end -> rematch loop.
- `npm run sim:soak` runs a longer no-browser endurance test across every launch mode and three deterministic seeds, driving human input, bots, magnets, dash, powerups, obstacles, scoring, feedback events, round flow, match restarts, and integrity assertions for finite positions/timers plus carried-marble ownership. Latest no-browser aggregate run at `2026-06-22T12:27:42.467Z` wrote `outputs/sim-soak-no-browser.json` with `15` mode/seed runs, `2,700` simulated seconds, `81,000` fixed steps, `6` match ends, `15,772` bank events, `17,789` pickup/powerup events, and `3,515` hit/steal/knockoff events; before the fix it caught a non-finite Survival eliminated-player respawn sentinel.
- `npm run sim:perf` records a no-browser all-mode CPU budget into `outputs/sim-performance-smoke.json`. Latest no-browser validation run at `2026-06-22T12:27:44.108Z` simulated `10` runs, `22,500` fixed steps, and `750` sim seconds with `12,892` gameplay feedback events at `1023.2x` real time against a `60x` minimum; slowest run was King Magnet at `675.1x`.
- `npm run aa:readiness` performs a no-browser static AA candidate audit and writes `outputs/aa-readiness-smoke.json`. Latest no-browser validation run passed at `2026-06-22T13:21:38.424Z` with `77` checks, `0` blockers, and `0` warnings after adding player Motion control, Color Assist identity markers, HUD objective live-status and carried-meter semantics, explicit magnet-mastery input coverage, neutral touch-dash memory coverage, haul-weight speed-risk coverage, pre-contact powerup/obstacle affordance labels, later-round advanced powerup depth, visible menu powerup ramp, root crash recovery with public support-code intake, no-browser accessibility static smoke, no-browser mobile layout/touch static smoke, first-round Pull / Carry / Bank coach strip, pre-round mode briefing, competitive race-status chip, carry-risk banking and steal-target advice, rim-danger readability, right-thumb action-state readability, mode-aware results recap, result mastery badges, post-match Unlock & Equip reward CTA, distinct bot personalities, daily streak retention, next-unlock progression targeting, local mode records, no-browser simulation performance budget, safe-gate sim-soak evidence, deployed build/menu Candidate stamp evidence hardening, executable negative evidence-template guard, quick-bank streak mastery loop, fair privacy/support launch copy, local data reset control, public IP/reference safety, player SFX volume control, runtime removed-music cache/DOM guard, player SFX preview control, phone haptics preview control, cluster milestone callouts, explicit browser-automation opt-in, deploy-monitor coverage, release-status coverage, clean failure-exit coverage, live removed-music verification, source-fingerprint provenance, app-shell service-worker resilience with built-bundle registration coverage, executable service-worker behavior smoke coverage, physical-device QA evidence gate, Render hosting-config gate, human AA review evidence gate, release evidence template generator, Candidate stamp release-proof check, broader `audio/music.*` regression checks, and live preflight coverage for all common removed music extensions. It verifies stack/architecture split, mode lineup, 90-second rounds, vertical-slice coverage, long soak coverage, no-browser sim throughput, bot difficulty, readable bot personalities, phone touch dash from neutral, magnet/carry/combat tuning including full-haul speed risk, quick-bank scoring, first-round launch powerups, later-round advanced powerups, progression with in-game marble skin rendering, daily streaks, next-unlock goals, local mode bests/wins/matches, menu/HUD/touch/action surfaces, no-browser mobile layout/touch contract, first-time coach strip, pre-round mode briefing, competitive race status, carry-risk and steal-target advice, rim-danger advice, result recap advice, Color Assist non-color identity markers, player Auto/Reduced/Full motion control, no-browser accessibility contract, solo pause/resume safety, root error fallback, recovery code, and support intake, moment-to-moment feedback callouts, SFX-only audio plus volume/preview controls, local data reset, phone haptics plus preview controls, PWA app-shell caching and registration in `dist/assets`, service-worker behavior, visual juice hooks, fair no-ads/no-payments/privacy/support copy, public IP-safety, authoritative online safety, no-browser validation safety, explicit browser-smoke opt-in safety, no-browser release-status reporting with conditional next actions, clean failure exits for release monitors, physical-device evidence gating, human subjective review gating, Candidate stamp provenance, copied-template rejection, CI no-browser coverage, release preflight order, launch metadata, production build provenance, source fingerprint, docs, Render blueprint presence, no-browser deploy monitoring, and always-on backend configuration.
- `.github/workflows/ci.yml` now runs the same safe `npm run validate:no-browser` gate on push and pull request using Node 22, installs both root and `server/` dependencies, and uploads `outputs/` as CI artifacts. This keeps CI aligned with the expanded local release evidence instead of only checking typecheck/build.
- Server `npm run build`, `npm run nanoid:smoke`, and full `npm audit` pass locally. The previous `@colyseus/core@0.16.24` -> `nanoid@2.1.11` advisories are cleared with the local `server/vendor/nanoid-compat` package, which preserves Colyseus 0.16's callable CommonJS import shape while using crypto-backed Nano ID-compatible generation. The nanoid compatibility smoke now verifies server dependency/override wiring, CommonJS callable exports, ESM exports, URL-safe requested/default id lengths, helper exports, crypto-backed randomness, no `Math.random`, and uniqueness sanity without browser automation.
- `npm run online:smoke` now performs a repeatable Colyseus health/join/snapshot check and writes `outputs/online-smoke.json`. Latest no-browser validation rerun at `2026-06-22T05:05:49.828Z` against `ws://127.0.0.1:2568` saw `/health` in 11 ms, joined in 12 ms, and got the first snapshot in 14 ms (`4` players, `56` marbles, `intro` phase).
- `npm run online:modes:smoke` passes locally against `ws://127.0.0.1:2568` and writes `outputs/online-modes-smoke.json`. Latest no-browser validation rerun at `2026-06-22T05:05:50.796Z` joined `Classic`, `Battle`, `King Magnet`, `Team Bank`, and `Survival`; verified authoritative `snapshot.mode`, expected round counts/timers, Team Bank team ids (`0,1,0,1`), Survival lives (`3,3,3,3`), and the `Magnet Burst` / `Shock Pulse` / `Heavy Core` pickup-only pool. It also passed mode isolation: a Survival `joinById` against an occupied Classic room was rejected with `Room mode mismatch: room is classic, request was survival`, then Survival created a distinct room without server-side stack noise.
- `npm run online:disconnect:smoke` passes locally against `ws://127.0.0.1:2568` and writes `outputs/online-disconnect-smoke.json`. Latest no-browser validation rerun at `2026-06-22T05:05:51.882Z` joined two clients to the same room, left the first client while it was sending movement/magnet/dash input, verified the abandoned seat became a bot with magnet inactive, then verified a replacement client reclaimed that abandoned seat (`0 -> bot -> human`, replacement joined in 67 ms).
- `npm run release:verify` now writes `outputs/release-readiness.json` as the final public-release gate. Latest real-endpoint run at `2026-06-21T23:57:13.602Z` failed correctly because the release candidate is still uncommitted locally, the live frontend is missing build metadata, and live backend `/health` does not expose the current commit. It used the passing 22-step local report with progression, PWA asset, dist metadata, mobile perf, privacy/support launch pages, visible mobile menu launch links, full first-viewport mode-card coverage, and server audit evidence (`outputs/launch-check-progression-mobile-perf.json`) before failing on public deployment state. `release:verify` now fetches live `build.json` and backend `/health` before starting Chrome/CDP; if either static version check fails, it records live web/online checks as skipped with `Chrome/CDP was not started`. After a versioned live frontend and backend are deployed, the script must pass live web smoke, Classic join, all-mode snapshots, and disconnect takeover / replacement-seat reuse before public release.
- `npm run live:version` is now available as a safe public-deploy preflight that checks frontend `build.json`, verifies removed `audio/music.*` paths return `404`/`410` or only tiny legacy silent tombstones, and checks backend `/health`. It writes `outputs/live-version-smoke.json`, compares both services against the expected git commit and source fingerprint, records `browserAutomation: false` / `cdpStarted: false`, and exits before any Chrome/CDP work. It intentionally fails if live `https://magnet-marbles.onrender.com/audio/music.mp3`, `.ogg`, `.wav`, `.m4a`, `.aac`, or `.flac` is served as any real music-size file. Latest no-browser release-status child run at `2026-06-22T13:28:41.731Z` failed correctly because live web/backend still report source fingerprint `e2c5454ca1348f60` instead of current local `f7e0a662220fc3ae`; the removed music check still passed, with `music.mp3` as the existing `1,370` byte tombstone and the other five variants returning `404`.
- `npm run deploy:monitor` is now available as the no-browser live deploy monitor and writes `outputs/deploy-monitor.json`. It runs `live:version`, live Classic join/snapshot, live all-mode snapshots/mode-isolation, and live disconnect takeover/replacement-seat reuse against Render without starting Chrome/CDP. `.github/workflows/deploy-monitor.yml` runs the same monitor manually or every six hours and uploads the reports. Latest run at `2026-06-22T06:36:37.432Z` passed with matching live frontend/backend metadata, source fingerprint `e2c5454ca1348f60`, live Classic, live all-mode snapshots/mode isolation, and live disconnect takeover/replacement-seat reuse. The monitor and live smoke scripts use regression-checked clean nonzero `exitCode: 1` failure exits for stale public endpoints instead of crashing with a Windows libuv assertion, so the report remains strict and readable.
- `npm run release:status` is now available as a no-browser launch dashboard and writes `outputs/release-status.json`. Latest run at `2026-06-22T13:28:41.731Z` failed correctly with `browserAutomation: false` / `cdpStarted: false` because this working tree is dirty, live web/server still report `e2c5454ca1348f60` instead of current `f7e0a662220fc3ae`, physical-device evidence is missing, human AA review evidence is missing, and the latest `release:verify` report did not start/reuse CDP. The refreshed local `dist/build.json` and full `validate:no-browser` proof now match `f7e0a662220fc3ae`. The dashboard now reports dirty-file scope and unchecked live Render service-config evidence as a visible warning unless `RELEASE_STATUS_REQUIRE_HOSTING_LIVE=1` is set. Its `nextActions` list is dependency-aware: while the live candidate is stale, it suppresses "fill device/human evidence" instructions and instead tells reviewers not to collect or submit evidence until `live:version` passes and `reviewer-handoff.md` is regenerated. Its child `outputs/release-status-live-version.json` still confirms removed background music is not live: `music.mp3` is only the `1,370` byte tombstone, and `.ogg`, `.wav`, `.m4a`, `.aac`, and `.flac` return `404`.
- `npm run device:qa` now validates `docs/DEVICE_QA_CHECKLIST.md` and optionally validates `outputs/device-qa-evidence.json` without browser automation. `release:verify` requires that structured physical-device evidence by default (`RELEASE_REQUIRE_DEVICE_QA=0` is local-audit-only), covering Android Chrome and iOS Safari install/offline behavior, touch controls, safe-area/readability, midrange Android performance, screen-reader/focus, haptics/audio, online recovery, and capture of any `MM-` crash support code if the recovery screen appears. The evidence schema now also requires deployed `/build.json` metadata and visible menu Candidate stamp proof; `npm run evidence:templates` generates `outputs/evidence-templates/reviewer-handoff.md` for reviewers, and `npm run evidence:negative` regression-checks that a copied generated template fails with placeholders when evidence is required.
- `npm run hosting:smoke` now validates `render.yaml` without browser automation. `release:verify` requires live Render service-config evidence by default (`RELEASE_REQUIRE_HOSTING_CONFIG=0` is local-audit-only), so the backend cannot silently remain on free-tier hosting when the repo blueprint expects `starter`.
- `npm run human:review` now validates `docs/HUMAN_AA_REVIEW_CHECKLIST.md` and optionally validates `outputs/human-aa-review-evidence.json` without browser automation. `release:verify` requires that structured human AA review evidence by default (`RELEASE_REQUIRE_HUMAN_REVIEW=0` is local-audit-only), covering fun-in-30-seconds, marble feel, mobile UX, art/audio juice, accessibility/comfort including crash support-code intake, launch risk, and final ship/no-ship decision. The evidence schema now also requires deployed `/build.json` metadata and visible menu Candidate stamp proof; `npm run evidence:negative` regression-checks that a copied generated template fails with placeholders when evidence is required.
- `npm run evidence:templates` now writes current-candidate starters to `outputs/evidence-templates/device-qa-evidence.template.json` and `outputs/evidence-templates/human-aa-review-evidence.template.json` without touching the strict evidence paths. The generated reviewer handoff now tells reviewers to capture any visible `MM-` crash support code before reload/reset and adds a Review Start Gate: reviewers must wait for `npm run live:version` to pass for the current commit/source fingerprint before collecting phone QA or human AA evidence. `npm run evidence:negative` then passes only if those generated templates are rejected by the strict device QA and human AA validators with evidence required, including deployed `build.json` and visible Candidate stamp proof.
- Live backend smoke found Render cold-start behavior: first concurrent join failed with `socket hang up` while `/health` took 22.478 s; retry after warm joined in 440 ms (`4` players, `56` marbles, `intro` phase).
- Client-side `/health` warm-up now converts Colyseus endpoints to health URLs before matchmaking. Latest live `npm run online:smoke` inside `launch:check` against `wss://magnet-marbles-server.onrender.com` passed with `/health` in 192 ms, joined in 285 ms, and first snapshot in 331 ms (`4` players, `56` marbles, `intro` phase).
- Previous live backend all-mode monitor failed at protocol level: `npm run deploy:monitor` reported at `2026-06-22T02:57:58.724Z` that `live:online-modes` allowed a Survival `joinById` into an occupied Classic room. The current monitor passed at `2026-06-22T06:36:37.432Z`, proving the deployed backend now rejects wrong-mode private-room joins and serves current source-fingerprint metadata.
- `npm run live:smoke` checks the public web deployment at `https://magnet-marbles.onrender.com/` and writes `outputs/live-web-smoke.json` plus `outputs/live-web-smoke.png`. It is intentionally behind the no-browser deploy proof until `MM_ALLOW_BROWSER=1` is provided; `npm run live:version` and `npm run deploy:monitor` now prove the public frontend/backend are current without opening Chrome.
- Desktop menu/game screenshots captured with Playwright.
- Mobile first-run collect and bank-prompt screenshots captured with Playwright.
- Mobile menu and online recovery screenshots captured with Playwright: `magnet-web-mobile-menu-online-visible.png`, `magnet-web-mobile-online-retry-state.png`, and `magnet-web-mobile-online-retry-success.png`.
- Mobile accessibility snapshot confirms selected menu controls expose pressed states and gameplay action buttons expose labels/disabled state.
- Fresh mobile menu screenshots captured after accessibility/readability passes: `magnet-web-mobile-menu-accessible-states.png` and `magnet-web-mobile-menu-scrim-clean.png`.
- Mobile menu DOM smoke confirms the visible shortcut footer is removed, the screen-reader-only controls guidance is clipped to 1px, and the 390x844 menu does not overflow.

## Fixed This Pass

- Mobile HUD layout no longer places the carried-marble meter under the touch controls.
- Mobile score, timer, and quit controls now have separate edge zones instead of overlapping.
- Added a compact objective chip tied to match state: collect, bank, sudden death, full cluster, or low-time banking.
- Sound, SFX volume, Test SFX preview, Haptics, Test Haptics preview, Color Assist, Motion, and quality settings now persist in `localStorage`.
- Added a first-run tutorial assist for solo play: bots are slowed and capped during the opening window until the human banks, the HUD teaches collect then bank, and the human goal pulses while carrying marbles.
- Added focused simulation tests for tutorial bank completion, bot bank-score caps, and bot instant-score caps.
- Upgraded Vite / React plugin tooling to clear dev dependency audit findings and kept the production build passing.
- Reworked the mobile menu so Single Player, Play Online, room-code access, and the online status surface are visible in the first portrait viewport.
- Added online join timeout, stale-connection guards, retryable error copy, and cold-start status text for Render wake-ups.
- Replaced the server's `colyseus` meta dependency with pinned `@colyseus/core@0.16.24` and `@colyseus/ws-transport@0.16.5`, preserving the 0.16 client protocol while removing the old optional peer dependency bulk.
- Added reduced-motion handling for DOM animations, the animated menu marbles, gameplay camera parallax, and ambient motes.
- Added visible keyboard focus rings and accessible selected/disabled/label states for menu, HUD, and touch-action controls.
- Expanded deterministic sim coverage for same-seed spawns, steal transfer, sudden-death tie handling, and paint plus double-score banking.
- Fixed a steal-regression bug: player-player collisions now choose the aggressor from pre-impact speed, before collision impulse transfers speed to the victim.
- Added a dependency-free Chrome/CDP `npm run perf:smoke` gate for mobile-size gameplay boot and frame pacing.
- Expanded sim coverage again for knockoff cluster drops, goal-block buttons, auto-goal rings, and server snapshot -> `NetView` compatibility.
- Added online server health warm-up before Colyseus matchmaking to reduce Render cold-start `socket hang up` failures, with tests for URL conversion, health retry, timeout copy, and browser network error normalization.
- Removed the always-visible menu shortcut list and replaced it with screen-reader-only keyboard/touch guidance attached to the main menu.
- Strengthened the mobile menu scrim and reduced animated menu-background opacity on narrow screens so the title, tagline, and primary actions read cleanly over the 3D marbles.
- Expanded launch-critical simulation coverage for bot banking target choice, powerup pickup respawn, disable-magnet snapshot transfer into `NetView`, and full three-round match progression.
- Added `npm run online:smoke`, a deploy-friendly online readiness check that warms `/health`, joins an arena with `colyseus.js`, waits for `welcome` plus the first snapshot, verifies player/marble counts, and records JSON evidence.
- Added `npm run a11y:smoke`, a repeatable keyboard accessibility smoke for menu, gameplay controls, results navigation, and online retry state.
- Added `npm run a11y:static`, a no-browser accessibility contract smoke wired into `validate:no-browser`, `aa:readiness`, and source-fingerprint provenance. It checks screen-reader guidance, focus-visible CSS, HUD status/meter semantics, accessible touch actions, pause/results labels and focus restore, reduced-motion CSS, comfort settings, pure model tests, and manual screen-reader/focus evidence requirements without opening Chrome.
- Added `npm run mobile:layout`, a no-browser mobile layout/touch contract smoke wired into `validate:no-browser`, `release:status`, `aa:readiness`, and source-fingerprint provenance. It statically checks the 390x844 phone target assumptions, safe-area CSS, first-screen play actions, candidate stamp, HUD/control non-overlap, right-thumb magnet/dash controls, thumb-sized actions, and keeps physical-phone evidence as the launch authority.
- Fixed the global gameplay keyboard listener so it no longer consumes Space/Enter-style interactions while focus is inside buttons or inputs; focused UI controls now retain native keyboard activation.
- Added `npm run launch:check`, an aggregate release gate that runs the web/server build, audit, test, accessibility, performance, local-online, and live-online checks and writes a single JSON report.
- Added `npm run assets:smoke` and integrated it into `launch:check` so generated SFX must exist in both source and production output before release.
- Added `npm run preview:smoke` and integrated it into `launch:check` so the built production bundle is browser-tested through Vite preview, with screenshot evidence and a guard against accidentally testing Vite dev modules.
- Added `npm run modes:smoke` and integrated it into `launch:check` so every requested mode is browser-tested through Vite preview with mode-specific objective/HUD assertions and screenshots.
- Added `npm run online:modes:smoke` and integrated it into `launch:check` against the local authoritative server, proving all requested modes survive Colyseus matchmaking/snapshot serialization before release.
- Added `npm run live:smoke` for post-deploy public-web verification and an opt-in `LAUNCH_CHECK_LIVE_WEB=1 npm run launch:check` mode to fail the aggregate gate if Render is serving a stale frontend.
- Replaced the old mode set with the requested My Street-style gameplay modes: `Classic`, `Battle`, `King Magnet`, `Team Bank`, and `Survival`.
- Added mode-specific rules instead of label-only modes: Battle scores successful steal impacts and big rim knockoffs, King Magnet awards the unique largest carrier every 2 seconds, Team Bank supports 2v2 shared scoring at either team goal, and Survival spends three lives before elimination.
- Aligned the normal powerup spawn pool to the vertical-slice trio: `Magnet Burst`, `Shock Pulse`, and `Heavy Core`, with short burst magnet force, radial carried-marble knock-loose, and heavy/slower collision tradeoff behavior.
- Added a later-round powerup ramp so multi-round matches preserve the clean Pull / Pulse / Heavy first round, then introduce Plus Five, Turbo, Paint, Super Magnet, Double Score, and Jam depth after players understand the core loop.
- Extended local/server snapshot data with mode id, team id, survival lives, and new active buff state so offline and online render through the same HUD path.
- Added generated sample SFX for pickup, bank, hit, steal/shock pulse, Magnet Burst, and fall/rim events, wired defensively over the existing synth engine.
- Added procedural magnet tether rendering so active magnet pulls are visible from shooter marbles to nearby loose marbles during gameplay.
- Updated `README.md` and added `docs/ASSET_BUDGET.md` so web gameplay, online support, powerups, and generated asset spend are documented accurately.
- Rebuilt the main menu into a higher-polish arcade shell: mode showcase, per-mode accent badges, candy-marble brand lockup, selected-mode rules, Round 1 / Later rounds powerup strip, clearer player/options controls, stronger primary/online CTAs, private-room affordance, and status handling.
- Reworked tablet/mobile menu breakpoints so the menu stacks cleanly below 920px, hides stale card-era CSS, keeps Single Player/Play Online visible in the 390x844 first viewport, removes visible mobile scrollbars, and prevents desktop tagline/panel overlap.
- Added production menu screenshot capture to `npm run preview:smoke` plus configurable preview viewport env vars for desktop/mobile UI regression checks.
- Changed browser smoke orchestration so `npm run launch:check` starts one shared headless Chrome/CDP instance and passes `MM_CDP_PORT` / `MM_REUSE_CDP=1` into a11y, preview, modes, perf, and optional live-web smokes. The standalone browser smokes now share the same default CDP port, reuse an already-running CDP browser when available, and use process-tree cleanup on Windows when they own Chrome; `LAUNCH_SHARED_CDP=0` restores isolated browsers when debugging a single flaky check.
- Added `npm run live:online:modes` and an opt-in `LAUNCH_CHECK_LIVE_ONLINE_MODES=1 npm run launch:check` gate so post-deploy verification covers every live backend mode, not just Classic.
- Added `outputs/` to `.gitignore` so local smoke screenshots/reports stay as evidence artifacts and cannot accidentally ship as source.
- Added launch-critical sim coverage for human magnet input under disable/expiry timing and one-frame dash input gated by cooldown.
- Added deterministic all-mode seeded soak coverage that advances bots, magnets, banking, obstacles, powerups, sudden-death flow, and survival state while asserting finite positions and valid carried-marble ownership.
- Fixed server disconnect cleanup so abandoned human seats clear movement, held magnet, one-shot dash, one-shot powerup activation, and pending server input before bot takeover.
- Added `npm run online:disconnect:smoke` and integrated it into `launch:check` so local authoritative multiplayer now verifies bot takeover and replacement-seat reuse after a player disconnects mid-input.
- Added `npm run soak:smoke` and integrated it into `launch:check` so the production preview now gets a longer browser-driven play soak with console error/warning capture and page-alive assertions.
- Fixed muted-session audio wake behavior so the app does not create/resume an `AudioContext` when sound is disabled, eliminating autoplay-policy warning noise during muted automated and player sessions.
- Added `npm run perf:mobile:smoke` plus opt-in `LAUNCH_CHECK_MOBILE_PERF=1 npm run launch:check` coverage for a 390x844, 4x CPU-throttled mobile gameplay sample.
- Changed first-run settings so phone-sized/coarse-pointer/low-memory devices default to the Lite performance profile, while desktop defaults to High and users can still switch profiles manually.
- Capped High-mode DPR on phone-sized/coarse-pointer viewports and removed ambient motes from phone High mode, preserving the premium lighting path while avoiding the previous throttled mobile p95 miss.
- Added `npm run release:verify`, a post-deploy public-release gate that requires a clean candidate, a recent passing local launch report with mobile perf, live frontend commit/build metadata, live backend health commit, and live backend protocol checks before marking the public web version releasable.
- Added `npm run metadata:smoke` and integrated it into `launch:check` so the web app's title, description, viewport, PWA manifest, theme color, and icon references are validated before release.
- Kept `npm run assets:smoke` focused on shipped gameplay SFX only; background music is no longer a required launch asset.
- Changed shared-CDP browser smokes to create and close a fresh tab per smoke run inside the one shared headless Chrome process, and hardened `npm run browser:guard` so wrapper detection, `MM_REUSE_CDP`, `closeCdpPage`, client close, and browser cleanup stay enforced without launching Chrome. This preserves the no-browser-spam behavior while preventing stale tabs/documents from inflating memory and destabilizing later performance checks.
- Cleared the remaining server `npm audit` advisories without jumping Colyseus protocols by adding a local `nanoid@3.3.8` compatibility package and switching `launch:check` from high-threshold server audit to full `npm audit`.
- Added `npm run icons:generate`, reproducible generated PNG launch art (`180`, `192`, `512`, maskable `512`, and `1200x630` social card), upgraded `index.html` / `manifest.webmanifest` mobile install metadata, and tightened `metadata:smoke` so the production build must contain correctly sized PNG assets before release.
- Extended `npm run release:verify` so post-deploy public release now requires `outputs/release-online-disconnect-smoke.json`, proving abandoned-seat bot takeover and replacement-seat reuse on the live backend in addition to Classic and all-mode joins.
- Added a local-first progression loop: offline match stars, deterministic daily challenge with first-win bonus, six unlockable/equippable marble skin/trail cosmetics, match-end reward copy, persistent localStorage state, unit coverage for rewards/unlocks/daily determinism, and production preview smoke assertions for the progression UI.
- Added daily challenge streak tracking to the local progression loop. Consecutive UTC daily wins now update current/best streaks, the daily card previews the next streak value, match rewards call out streak progress, and progression/store/AA-readiness coverage guards the behavior.
- Added a next-unlock progression target in the menu and post-match reward panel, backed by unit coverage and AA-readiness static checks, so earned stars point to a clear cosmetic goal.
- Added local mode records for best score, wins, and matches played. The selected mode card now shows its record, match rewards call out new bests, the store avoids duplicate reward writes for the same run, and unit plus AA-readiness checks cover the path.
- Added a two-step Reset data control in the main menu Options area. It clears only Magnet Marbles local settings, tutorial state, offline stars, daily streaks, records, and skin/trail unlocks, keeps unrelated site storage intact, updates Privacy/Support copy, and is covered by store, metadata, and AA-readiness checks.
- Added `npm run sim:perf`, a no-browser all-mode simulation throughput budget that writes structured evidence, runs inside `validate:no-browser`, and is statically covered by AA readiness. This catches severe authoritative-sim CPU regressions before browser or phone profiling.
- Tightened the mobile lobby after the progression pass: brighter menu scrim/panels, clearer text contrast, and Single Player / Play Online moved ahead of the long mode/settings controls so a 390x844 phone viewport shows the primary action immediately.
- Optimized the mobile-safe Lite renderer by using 1.0 DPR, disabling shadow maps in mobile/lite, lowering collectible marble geometry, skipping the collectible glow-halo instanced pass, and keeping trail geometry focused on the human marble in Lite mode.
- Added shipped static privacy and support pages, small menu links to them, and metadata smoke coverage so production builds fail if those launch-readiness pages are missing from `dist/`.
- Tightened the shipped privacy/support pages and metadata smoke so launch builds explicitly verify no ads, analytics trackers, payments, in-app purchases, subscriptions, loot boxes, or paid cosmetics, plus safe bug-reporting guidance and exact touch/audio/haptics support copy.
- Moved Privacy / Support links into the mode panel header, compacted the mobile mode grid to remove bottom-overlay clipping, added keyboard focus treatment, and extended `npm run preview:smoke` to fail if launch links disappear or any requested mode card is clipped in the 390x844 production menu.
- Added a testable right-thumb gesture model for the current web controls: direct-drag movement stays on the 3D playfield, the right side can hold magnet, and right-side tap/flick maps to dash while the visible buttons remain available. Added pure unit tests for tap/flick classification, drag-vector clamping, shared touch movement, magnet hold, dash/activate edge clearing, and direct-drag target reset.
- Reduced the invisible right-thumb gesture zone from a full-height blocker to a lower-right thumb area, preventing it from sitting over the HUD/quit region while preserving hold-magnet and tap/flick-dash behavior. Updated screen-reader and visible touch copy to match the direct-drag web controls.
- Tightened the core magnet-mastery feel: playfield drag no longer auto-activates magnet, the R3F input bridge forwards only explicit magnet input, the first-round objective now teaches holding magnet near candy marbles, and unit/AA-readiness coverage prevents movement-only input from silently becoming magnet input again.
- Added lightweight in-world affordance badges for powerups and later-round obstacles. Pickups now show short metadata-driven labels such as MAG, PULSE, and HEAVY before collection; goal-block buttons and auto-bank rings show BLOCK/AUTO labels; and no-browser tests plus AA-readiness coverage keep the labels short, readable, and synced when pickups respawn with a new type.
- Added `npm run validate:no-browser` and documented it as the safe local validation path for code/UI work that must not spawn Chrome or Vite preview tabs.
- Fixed Team Bank sudden death so overtime compares tied teams instead of tied teammates. Added regression coverage for a uniquely leading team ending normally and a true team tie staying alive only until one team breaks the tie.
- Hardened authoritative input handling by sanitizing movement axes and one-shot booleans before they enter the shared `World` sim, client `NetView`, or Colyseus room state, preventing malformed online input from producing non-finite physics values. `NetView` now sends a copied input payload before clearing one-shot actions. Expanded `npm run validate:no-browser` to compile the server bundle and added regression coverage for malformed/clamped player input plus outbound network input snapshots.
- Fixed online input flushing so continuous `NetView` input is actually throttled around 25Hz while dash/powerup one-shot actions still flush immediately and survive render ticks until sent. Added regression coverage for continuous throttling and immediate one-shot delivery.
- Preserved auto-goal ring target-goal ids through server snapshots and `NetView`, removing an online/offline metadata drift that could make ring guidance or future ring effects point at the wrong goal. Existing snapshot-to-`NetView` regression coverage now asserts ring target parity.
- Preserved auto-goal ring spin phase through snapshots so online ring arrow motion mirrors the authoritative sim instead of resetting every server update. Snapshot-to-`NetView` coverage now asserts ring spin parity.
- Removed the loud generated background song and the fallback procedural melody so sound is short gameplay feedback only. Runtime music playback is gone; `public/audio/music.*` no longer ships, the app boot guard blocks stale `audio/music.*` playback attempts, purges old cache entries/media elements, and the service worker purges/responds `410 Gone` for old removed music paths.
- Added a persisted, clamped SFX volume control with a quieter `28%` default and a global output trim, applied through the WebAudio master gain and exposed in both the main menu and pause options. A one-time `audioTuningVersion` migration caps old saved loud SFX volumes to the new baseline while preserving current-version user choices. Added store/settings coverage plus an AA-readiness check so the volume control cannot disappear silently.
- Added Test SFX controls in the main menu and solo pause options so players can audition the current SFX volume without starting a round or reintroducing background music. Metadata and AA-readiness smokes now cover the support copy, menu/pause controls, and sample playback hook.
- Made end-of-match results and offline rewards mode-aware instead of raw individual-score sorting. Team Bank now ranks/rewards by shared team placement, Survival standings prioritize remaining lives, and Survival timer endings choose the highest-lives player before score. Added pure results tests plus a Survival timer regression.
- Hardened authoritative online rooms: matchmaking now filters `arena` rooms by requested `mode`, wrong-mode private room codes are rejected before slot assignment, the server no longer accepts client-driven round-advance messages, replacement human seats clear inherited bot input on join, `NetView.forceAdvance()` is a no-op online, and `online:modes:smoke` now proves occupied rooms do not cross-match different modes.
- Replaced the shell-chain `validate:no-browser` with a Node orchestrator that records `outputs/no-browser-check.json` and adds non-browser launch gates: web/server audit, dist metadata smoke, SFX asset smoke, temporary local authoritative-server smoke, all-mode online protocol smoke, disconnect-takeover smoke, and whitespace checks.
- Added `npm run dist:budget` to both `validate:no-browser` and `launch:check` so production payload size, gzipped JS, source-map/dev-client leakage, image/audio budget, and removed-background-music regressions are caught without browser automation.
- Extended `npm run assets:smoke` and `npm run dist:budget` so the removed background song is regression-checked as absent, not a playable music bed; either check fails if `public/audio/music.*` or `dist/audio/music.*` exists.
- Extended `npm run live:version` so public Render deploy preflight verifies all common removed `audio/music.*` variants, not just the original MP3 path, before any browser/CDP work can start.
- Added `npm run vertical:slice`, a focused no-browser contract test that locks the requested My Street-style web vertical slice against accidental mode, powerup, bot-count, cosmetic, daily-challenge, or magnet/carry tuning drift.
- Fixed HUD player selection so objective copy and carried-cluster UI use the actual `humanId` instead of assuming player slot `0`, which matters for online replacement seats and future non-zero-seat joins.
- Extended the vertical-slice contract from static configuration checks into a simulated core-loop proof: the human moves, magnetizes a marble, carries and banks it, bots produce competitive intent, the match reaches results, and a rematch starts cleanly.
- Tightened touch-control fidelity to the requested phone model: playfield drag now moves only, while tap/flick dash stays on the lower-right/right-control path. Added regression coverage so repeated direct-drag targets cannot set a dash edge.
- Fixed tap/flick dash from a neutral thumb state by remembering the last movement direction inside the authoritative sim. A no-direction tap no longer burns cooldown, while a neutral dash after movement launches in the last direction; the server seat reset path clears this direction memory on join/leave.
- Added `npm run build-info:smoke`, a no-browser production-bundle provenance guard that checks `dist/` contains the expected web build commit/branch metadata before local validation or launch checks can pass.
- Added `dist/build.json` emission from Vite and a `release:verify` static live-web version check, giving Render deploys a no-browser frontend provenance endpoint before deeper browser/gameplay smoke runs.
- Hardened `release:verify` so live frontend/backend build-version preflight runs before any browser work; stale or unreachable live endpoints now skip Chrome/CDP and mark deeper live web/online checks as skipped instead of opening tabs.
- Added `npm run aa:readiness`, a no-browser static AA-readiness audit, and wired it into `validate:no-browser` so core web launch requirements are checked without opening Chrome.
- Added `npm run sim:soak`, a longer deterministic all-mode simulation endurance test, and fixed Survival eliminated players so their `respawnTimer` remains finite (`0`) instead of using an `Infinity` sentinel.
- Upgraded GitHub Actions CI from build-only to full no-browser validation, including server dependency install and report artifact upload.
- Switched the Render backend blueprint from free tier to the paid always-on `starter` plan and made `npm run aa:readiness` fail if the authoritative backend regresses to free-tier hosting.
- Added browser vibration haptics for supported phones, with rate-limited patterns for pickup, bank, hit, steal, fall, knockoff, and powerup feedback plus tactile taps for magnet/dash/use controls. Added a persisted menu Haptics toggle and unit coverage for the haptic event patterns.
- Added Test Haptics controls in the main menu and solo pause options. A short phone-safe vibration pattern lets players preview tactile feel before or during a match, with focused haptics tests plus metadata and AA-readiness coverage.
- Added reduced-motion-safe camera-impact juice for big banks, steals, knockoffs, falls, paint, hits, and powerups. The shake is bounded for table readability, wired through the existing `FxEvent` drain, and covered by unit tests plus the visual-juice AA readiness check.
- Added transient low-chrome HUD feedback callouts for major banks, steals, knockoffs, falls, paint, and powerups. The feedback model ignores spammy pickups/minor hits, selects the highest-priority event per frame, clears itself quickly, and is covered by unit tests plus the AA readiness gate.
- Added solo pause/options lifecycle handling: Escape and the HUD Pause button freeze solo matches, tab-hide auto-pauses, controls clear on pause/resume/restart/menu, the pause overlay exposes Resume/Restart/Menu plus sound/haptics/quality toggles, focus enters Resume and restores to the prior element on close, and online matches remain unpaused. Store tests and the AA readiness gate cover the behavior.
- Added `npm run live:version`, a no-browser public staleness preflight for Render that checks only live frontend/backend build metadata, tolerates cold starts with a 60-second timeout, records structured endpoint failures instead of raw aborts, and is statically covered by the AA readiness gate.
- Added the requested three bot difficulty levels for solo play: Easy, Normal, and Hard are persisted settings, exposed in the menu and pause options, passed into local and daily worlds, and tune bot speed, retarget cadence, skill jitter, and aggression. Vertical-slice, store, sim, and AA readiness checks cover the feature.
- Upgraded progression cosmetics from trail-only presentation to visible marble skin/trail cosmetics: each unlock now has a skin color, accent, and finish; the in-game shooter marble shader uses the selected skin accent; the menu swatch shows a marble plus trail; and vertical-slice plus AA readiness checks prove the rendering path.
- Added an explicit browser-automation opt-in: any script that would launch a Chrome/CDP process or create CDP tabs now requires `MM_ALLOW_BROWSER=1`. `launch:check` fails at an early `browser:opt-in` step before doing build/server setup when the opt-in is missing; the expected-failure guard report is `outputs/launch-check-browser-opt-in-guard.json`. `release:verify` still performs static live preflight and no-browser live online checks, then records live web as skipped/failed if the browser opt-in is missing. `npm run browser:guard` verifies this without launching Chrome, scans browser-capable scripts and browser wrappers against an allowlist, fails if `validate:no-browser` starts calling browser smoke scripts, and now enforces CDP page cleanup plus shared-browser reuse so opt-in browser runs do not leave blank tabs behind.
- Added `npm run deploy:monitor` plus a scheduled/manual Deploy Monitor GitHub Action, giving Render deploys a browser-free live monitor for frontend/backend build metadata, Classic online join, all requested backend modes, and disconnect takeover.
- Added `npm run release:status`, a no-browser launch dashboard that summarizes dirty-tree state and scope, stale `dist/` build metadata, latest `validate:no-browser` proof, live web/server source fingerprints, removed-music live status, device QA evidence, human AA evidence, reviewer-template freshness, hosting config including unchecked live Render service-config warnings, and browser-gate readiness without opening Chrome/CDP. Its next-action list is now generated from failed checks instead of a fixed checklist, so current safe-gate/template proof does not produce stale rerun instructions.
- Added source-fingerprint provenance across frontend `build.json`, browser runtime build info, server `/health`, `build-info:smoke`, `source:fingerprint`, local online smokes, `launch:check`, `live:version`, `deploy:monitor`, and `release:verify`. This catches dirty-candidate drift where a live service reports the same git commit but lacks local code or shipped asset behavior.
- Added Color Assist as a persisted readability option. It adds P1-P4 text markers to the HUD/results and lightweight one-to-four white mesh pip badges above player marbles and goals, so ownership is readable without relying on color alone. Store/unit coverage and the AA readiness gate lock the feature in, and the first heavier implementation was rejected by the dist-budget smoke before shipping.
- Added a persisted Motion setting with Auto, Reduce, and Full modes. Auto follows `prefers-reduced-motion`; Reduce forces calmer camera shake, parallax, ambient motes, and menu motion; Full allows normal arcade motion. The setting is available in the main menu and solo pause options, covered by pure tests plus the AA readiness gate.
- Added carried-cluster milestone feedback at 3, 6, 10, and 18 carried marbles, with low-chrome HUD callouts, SFX, haptics, particles, and sim coverage proving the feedback triggers at thresholds instead of every pickup.
- Added a screen-reader-safe HUD objective status and ARIA carried-marble meter. The visible low-chrome HUD stays unchanged, while assistive tech gets polite objective updates and proper carried-count meter semantics with no per-second timer announcement spam.
- Added a first-round Pull / Carry / Bank coach strip inside the existing objective chip. It only appears while tutorial assist is active, advances from Pull to Bank once the player carries marbles, and disappears after the first successful bank.
- Added a pre-round mode briefing to the countdown: it shows the human player marker, mode-specific objective, and a three-step plan for Classic, Battle, King Magnet, Team Bank, and Survival without adding persistent HUD clutter.
- Added a compact live race-status chip so players can read whether they are leading, tied, or chasing a rival/team/lives lead without opening a scoreboard or covering the playfield.
- Added a compact carry-risk advice chip beside the carried-meter so players can read empty, build, sweet-spot, risky, urgent, and streak hauls without opening a tutorial overlay.
- Added steal-target guidance to the same carry-advice surface: when the player is empty and an opposing rival is carrying a valuable haul, Classic/Battle copy points to the loaded rival instead of only telling the player to collect more.
- Added a compact rim-danger warning chip that reacts when the player is fast, loaded, or on a final life near the table lip, with screen-reader status copy and no-browser AA coverage.
- Added a compact right-thumb action-state strip near the mobile controls so held powerups, dash cooldown, and magnet-pulling state are readable without opening a tutorial or covering the playfield.
- Added a tunable haul-weight movement risk so full carried clusters move slightly slower while staying responsive. This makes big clusters mechanically riskier, matching the carry-risk HUD advice and the requested easy-to-read/hard-to-master loop.
- Added a quick-bank streak mastery loop: repeat bank runs within the short timer award a visible +1/+2 per-marble bonus, feed HUD callouts, SFX, particles, haptics, camera impulse, online snapshots, and sim/UI/feedback/AA-readiness coverage.
- Added a compact mode-aware results recap after rounds/matches so Classic, Team Bank, and Survival players see why they won/lost and what to try next.
- Added a result Mastery Badge so match-end feedback can call out new personal bests, clutch banks, survivor wins, rival smashes, and near-miss next-run targets.
- Added a post-match Unlock & Equip reward CTA so players can claim a ready cosmetic directly from the results screen instead of backing out to the menu.
- Added readable solo bot personalities: Collector builds larger hauls, Bruiser hunts loaded rivals, Banker cashes out smaller hauls, and the roles are shown in menu/HUD/results plus serialized online.
- Added a structured human AA review gate so subjective fun, feel, art/audio, mobile UX, accessibility comfort, fair launch claims, and release risk are validated before public signoff instead of being implied by automated tests alone.
- Added `npm run evidence:templates`, a no-browser helper that stamps physical-device and human-AA evidence templates plus a reviewer handoff with the current commit/source fingerprint in a non-gating output folder. The strict evidence validators now reject TODO/TBD/placeholder values.
- Added `npm run evidence:negative`, a no-browser guard that generates the current templates and proves copied placeholders fail the strict physical-device and human-AA evidence validators before they can be mistaken for final signoff.
- Added a compact menu Candidate stamp plus support/checklist guidance so physical-device and human-AA screenshots can prove the exact tested commit/source fingerprint against `build.json`.
- Added `npm run ip:safety`, wired it into `validate:no-browser` and `aa:readiness`, and removed protected reference-game names from public/package launch copy while keeping internal design notes allowed.
- Expanded source-fingerprint coverage to include validation scripts, with `scripts/ip-safety-smoke.mjs` required so release provenance changes when launch-safety gates change.
- Hardened live-version, deploy-monitor, release-readiness, and online smoke failure exits so expected stale-public failures return clean nonzero status codes and structured reports instead of abrupt Windows network-handle assertions.
- Added `npm run clean-exit:smoke`, wired it into `validate:no-browser` and `aa:readiness`, and required it in the source fingerprint so release-monitor failure-path regressions change provenance and fail the safe gate.
- Added a localhost-safe PWA service worker registration plus app shell. It keeps navigations and `build.json` network-first for deploy freshness, caches hashed/static assets, launch art, icons, and SFX cache-first after first load, purges removed music paths, avoids cross-origin backend interception, and is covered by metadata, source-fingerprint, AA-readiness, and production-bundle registration smokes.
- Added a root React error boundary with a branded recovery screen, stable support code, Reload, and local-only Reset data action. The support page, metadata smoke, AA-readiness gate, and reviewer handoff now tell players/reviewers to include any visible `MM-` crash support code in bug reports before reload/reset.

## Open Gaps

1. Public Render deployment is not current for this working tree. It was current for the previous source fingerprint `e2c5454ca1348f60` by no-browser proof: `npm run live:version` and `npm run deploy:monitor` passed, and the old `/audio/music.mp3` URL served only the `1,370` byte legacy silent tombstone. `npm run release:status` is the authoritative current-state report for the local source fingerprint, dirty-file scope, safe-gate freshness, live frontend/backend fingerprint mismatch, missing physical-device evidence, missing human AA review evidence, and browser-gate status. The current candidate has not been committed/deployed. The actual backend service config now uses `healthCheckPath: /health`, but prior live checks reported Render plan `free` while the repo blueprint expects `starter`; upgrading it would improve cold-start reliability but may incur hosting cost.
2. Colyseus 0.16 protocol compatibility remains a maintenance risk. Server audit is clean through `server/vendor/nanoid-compat`, and the shim is now covered by `npm run nanoid:smoke`, `npm run validate:no-browser`, source-fingerprint provenance, AA-readiness, and the release-status required step list. Future agents should still migrate to an official matching 0.17+ server/client protocol once available.
3. Simulation and smoke coverage are stronger but still not complete for launch. The no-browser all-mode endurance soak is now part of `npm run validate:no-browser` and catches finite-state and ownership regressions, and post-deploy disconnect takeover is proven by `npm run deploy:monitor`, but physical-device soak beyond deterministic sim and 15-second headless browser soak is still needed.
4. Mobile install/offline resilience is stronger through the app-shell service worker and `npm run sw:smoke` now executes install/activate/fetch behavior without a browser, but it still needs physical-phone install/offline review through iOS Safari and Android Chrome because no-browser checks cannot prove OS-level PWA install UX or mobile browser cache behavior.
5. Mobile menu composition now passes the 390x844 screenshot smoke and the no-browser `mobile:layout` static contract, but it still needs physical-phone review for glare, OLED contrast, and notch/safe-area variance.
6. Performance evidence is better but still incomplete. Normal, throttled mobile, high-quality stress headless smokes, and the no-browser authoritative-sim throughput budget pass, but launch still needs browser Performance panel or SpectorJS capture on midrange Android-class hardware to verify real GPU/postprocessing behavior and thermal stability.
7. Accessibility has repeatable keyboard smoke coverage for menu, gameplay, results, online retry states, plus a no-browser static contract for screen-reader guidance, focus-visible CSS, ARIA status/meter semantics, touch-control names, reduced motion, comfort options, Color Assist non-color identity markers, player Motion control, HUD objective announcements, and carried-marble meter semantics. Launch still needs a human screen-reader pass and physical-device checks for focus visibility under mobile browser UI.
8. Browser automation was stopped after Chrome tabs were reported as intrusive. The no-browser validation path is the safe default, Chrome/CDP launch or tab creation now requires explicit `MM_ALLOW_BROWSER=1`, `npm run browser:guard` now scans browser-capable scripts plus the safe orchestrator without launching Chrome, `release:status` reports browser-gate readiness without launching Chrome/CDP, and `release:verify` performs static live preflight before any Chrome/CDP work, but the full browser gate still needs to be rerun deliberately before final release signoff.
9. Live Render service config is not yet proven in the current `release:status` output. The repo blueprint expects the backend on `starter`, but status defaults to a no-token blueprint check; run `RELEASE_STATUS_CHECK_HOSTING_LIVE=1 npm run release:status` for a warning-level live check or `RELEASE_STATUS_REQUIRE_HOSTING_LIVE=1 npm run release:status` before launch signoff.
10. Physical-device evidence is now release-gated but not yet captured in this workspace. Before public signoff, deploy the current candidate and get `npm run live:version` passing, rerun `npm run evidence:templates`, use the refreshed `outputs/evidence-templates/reviewer-handoff.md`, create `outputs/device-qa-evidence.json` from `docs/DEVICE_QA_CHECKLIST.md`, and run `DEVICE_QA_REQUIRE_EVIDENCE=1 npm run device:qa`.
11. Human AA review evidence is now release-gated but not yet captured in this workspace. Before public signoff, deploy the current candidate and get `npm run live:version` passing, rerun `npm run evidence:templates`, use the refreshed `outputs/evidence-templates/reviewer-handoff.md`, create `outputs/human-aa-review-evidence.json` from `docs/HUMAN_AA_REVIEW_CHECKLIST.md`, and run `HUMAN_AA_REVIEW_REQUIRE_EVIDENCE=1 npm run human:review`.
